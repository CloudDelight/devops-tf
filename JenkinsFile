pipeline {
    agent any
     tools {
      terraform "Terraform1.0.0"
      }
    stages {
        stage ('terraform plan') {
       steps {
           sh "terraform -v"
           sh "terraform init"
           sh "terraform plan"
       } 
    }
    }
}

